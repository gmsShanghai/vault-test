{
  "name": "paypal-advanced-integration",
  "description": "Sample Node.js web app to integrate PayPal Advanced Checkout for online payments",
  "version": "1.0.0",
  "main": "server/server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "killport 13361 && nodemon server/server.js",
    "format": "npx prettier --write **/*.{js,md}",
    "format:check": "npx prettier --check **/*.{js,md}",
    "lint": "npx eslint server/*.js --env=node && npx eslint client/*.js --env=browser"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "body-parse": "^0.1.0",
    "config": "^3.3.11",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "killport2": "^1.0.4",
    "lodash": "^4.17.21",
    "lowdb": "^7.0.1",
    "moment": "^2.30.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "colors-console": "^1.0.3",
    "nodemon": "^3.0.1"
  }
}
